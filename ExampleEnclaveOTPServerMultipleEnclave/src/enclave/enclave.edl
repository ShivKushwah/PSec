enclave {
    from "sealing/sealing.edl" import *;
    from "sgx_tstdc.edl" import *;

    trusted {
        /* define ECALLs here. */
        public int save_otp_secret(int value);
        public int get_otp_secret(void);
    };

    untrusted {
        /* define OCALLs here. */
        void ocall_print([in, string]const char* str);
        void ocall_print_int(int print_int);

    };
};
